# Python
__pycache__/
*.py[cod]
.venv/
venv*/
*.egg-info/

# Django
*.sqlite3
/backend/media/
/backend/staticfiles/

# Node / Next.js
node_modules/
.next/
out/
.vercel/

# Env & secrets  (never commit these)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/

# Snapshot PDFs are large; HTML + manifest ARE tracked deliberately.
data/source_snapshot/**/*.pdf

# Harvested YouTube metadata cache
data/youtube_meta.jsonl

# Import QA output — regenerable from the database with `verify_import`,
# so it is derived output rather than source.
**/import_review.csv

# Tool caches & local browser-testing artefacts
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.playwright-mcp/
*.log
.vscode/

# Raw hero photos; the web-ready crops live in frontend/public/hero/.
Slider-photos/

# Raw recognition screenshots; the web-ready crops live in frontend/public/recognition/.
/Recognition/
